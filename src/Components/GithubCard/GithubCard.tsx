import { ReactElement } from 'react';

type GithubCardProps = {
    name: string;
    description: string;
    link: string;
    image: string;
}

export const GithubCard = ({ name, description, link, image }: GithubCardProps): ReactElement =>
    <div></div>

